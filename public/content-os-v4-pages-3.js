pages['Ideas']=()=>`${header('Ideas','Backlog priorizado usando lo que tus datos dicen que puede funcionar.',`<button class="btn" id="newIdeaBtn">+ Crear idea</button>`)}<div class="grid ideas-grid">${ideas().map(i=>`<div class="card idea-card"><div class="pattern-head"><span class="chip">${i.format}</span><span class="idea-score">Potencial ${i.score}/100</span></div><div class="idea-title">${i.title}</div><p class="small-muted">${i.why}</p><div class="idea-meta"><span class="chip">${i.audience}</span><span class="chip">${i.intent}</span><span class="chip">${i.hook}</span></div><div style="display:flex;gap:8px;margin-top:14px"><button class="btn ghost" data-idea="${i.title}">Abrir</button><button class="btn">Pasar a guion</button></div></div>`).join('')}</div>`;
pages['Replicar']=()=>{const v=content.find(x=>x.id===selectedId)||published()[0];return `${header('Replicar','Descomponé un ganador: mantené las variables que explican el resultado y cambiá el tema.')}
<div class="replicate-layout"><div class="card replicate-source"><div class="panel-title"><h3>Contenido fuente</h3><small>Score ${scoreVideo(v)}</small></div><div class="video-preview"><div class="play">▶</div></div><strong>${v.title}</strong><p class="small-muted">${v.format} · ${v.topic} · ${v.duration}s</p><div class="metric-grid"><div class="metric-box"><span>Save rate</span><strong>${pct(rate(v.saves,v.reach))}</strong></div><div class="metric-box"><span>Share rate</span><strong>${pct(rate(v.shares,v.reach))}</strong></div><div class="metric-box"><span>Watch ratio</span><strong>${pct(rate(v.avgWatch,v.duration))}</strong></div><div class="metric-box"><span>Follow rate</span><strong>${pct(rate(v.follows,v.reach))}</strong></div></div><select class="select" id="sourceSelect" style="width:100%;margin-top:14px">${published().map(x=>`<option value="${x.id}" ${x.id===v.id?'selected':''}>${x.title}</option>`).join('')}</select></div>
<div class="replicate-workbench"><div class="card keep-card"><div class="panel-title"><h3>Mantener</h3><span class="chip green">patrón probado</span></div>${[['Formato',v.format],['Hook',v.hook],['Duración objetivo',`${Math.max(25,v.duration-5)}–${v.duration+5}s`],['Intención',v.intent],['Audiencia',v.audience]].map(x=>`<div class="variable-row"><span>${x[0]}</span><strong>${x[1]}</strong></div>`).join('')}</div><div class="card change-card"><div class="panel-title"><h3>Cambiar</h3><span class="chip amber">variables nuevas</span></div>${[['Tema','Nuevo'],['Ejemplos','Nuevos'],['CTA','Variante'],['Número / ángulo','Test A/B'],['Visual principal','Nuevo']].map(x=>`<div class="variable-row"><span>${x[0]}</span><strong>${x[1]}</strong></div>`).join('')}</div><div class="card variant-list"><div class="panel-title"><h3>Variantes recomendadas</h3><button class="btn" id="regenVariants">✦ Generar nuevas</button></div><div id="variants">${variantHTML(v)}</div></div></div></div>`};
